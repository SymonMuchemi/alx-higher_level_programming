#!/usr/bin/python3
def raise_exception_msg(message=""):
    """raises a named exception

    Args:
        message (str, optional): name of the error. Defaults to "".

    Raises:
        NameError: _description_
    """
    raise NameError(message)
