-- lists all records of the table second_table 
-- Conditions:
--  display the score followed by the name 
--  record should be ordered by the score (top first)
SELECT score, name FROM second_table ORDER BY score DESC;
