#!/bin/bash

# Check if the $PYFILE variable is set
if [ -z "$PYFILE" ]; then
    echo "Error: PYFILE environment variable is not set."
    exit 1
fi

# Run Python script using the value stored in $PYFILE
python3 "$PYFILE"
